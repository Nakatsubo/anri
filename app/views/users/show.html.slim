- set_meta_tags title: @client.user.name + " | ANRI", description: "タイムライン"
.content.timeline
  .tit
    h1
      span #{@client.user.name}
  .main
    .banner
      = render 'components/users/banner'
    .wrapper
      .profile
        = render 'components/users/main_profile'
      .timeline
        = render 'components/users/timeline'
